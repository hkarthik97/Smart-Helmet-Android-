<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">
    <activity
        android:id="@+id/mainActivity"
        android:name="prac.zed.smarthelmet.MainActivity"
        android:label="activity_login"
        tools:layout="@layout/activity_login" />
    <activity
        android:id="@+id/forgotActivity"
        android:name="prac.zed.smarthelmet.ForgotActivity"
        android:label="activity_forgot"
        tools:layout="@layout/activity_forgot" />
    <activity
        android:id="@+id/profileActivity"
        android:name="prac.zed.smarthelmet.ProfileActivity"
        android:label="activity_profile"
        tools:layout="@layout/activity_profile" />
</navigation>